[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "channel-contribution-rlvr"
version = "1.0.0"
description = "API-only business analysis environment and deterministic final-answer RLVR rewards"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
include = ["rlvr_business*", "examples*"]
